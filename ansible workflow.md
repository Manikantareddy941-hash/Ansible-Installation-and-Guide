🧠 Now lock this sequence (simple + correct)

1️⃣ You run command/playbook on master
                ⬇
2️⃣ Ansible reads inventory (server list)
                ⬇
3️⃣ Connects to each server via SSH
                ⬇
4️⃣ Executes tasks on servers
                ⬇
5️⃣ Collects output back to master

In one clean line:

Command → Inventory → SSH → Execution → Output
